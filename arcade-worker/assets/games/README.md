# Local ROMs (not committed)

This directory is intentionally **gitignored** to avoid committing copyrighted ROMs/BIOS files.

Place your game files here (for example `*.zip`, `*.nes`, `*.gba`), then run the stack via
`deploy/container-stack.sh` using `WORKER_DEFAULT_GAME` or `WORKER_GAMES`.

